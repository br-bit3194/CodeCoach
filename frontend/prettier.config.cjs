module.exports = {
   semi: true, // Use semicolons
   singleQuote: true, // Use single quotes
   printWidth: 100, // Line wrap after 100 characters
   tabWidth: 3, // Use 2 spaces for indentation
   useTabs: false, // Use spaces, not tabs
   trailingComma: 'es5', // Add trailing commas where valid in ES5 (objects, arrays, etc.)
   bracketSpacing: true, // Add spaces inside object braces
   jsxSingleQuote: false, // Use double quotes in JSX
   arrowParens: 'always', // Always include parentheses around arrow function params
   endOfLine: 'auto', // Use system default for line endings
};
